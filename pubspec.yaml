name: favoriti
description: "Explore produtos, marque favoritos e gerencie suas escolhas"

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  result_dart: ^2.0.0
  firebase_core: ^3.8.1
  firebase_crashlytics: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
